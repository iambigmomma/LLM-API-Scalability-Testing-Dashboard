/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow development origins to fix cross-origin warnings
  allowedDevOrigins: ['165.227.40.179', 'localhost', '127.0.0.1'],
}

module.exports = nextConfig 